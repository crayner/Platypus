{% trans_default_domain 'System' %}
{% dump %}
{% include 'Default/panelStart.html.twig' with {header: 'notification.event.header', name: tabManager.entity.event, transDomain: 'System'} %}

<div class="row row-header">
    <div class="col-6">{{ 'notification.listener.person.label'|trans }}</div>
    <div class="col-2">{{ 'notification.listener.receive_notification_email.label'|trans }}</div>
    <div class="col-2">{{ 'notification.listener.scope.label'|trans }}</div>
    <div class="col-2">{{ 'actions'|trans }}</div>
</div>
    {{ collection_widget(form.notificationListeners) }}
{% include 'Default/panelEnd.html.twig' %}
{% do form.notificationListeners.setRendered %}