{% trans_default_domain 'Person' %}

{% include 'Default/panelStart.html.twig' with {header: 'family.children.title', name: manager.entity.name, transDomain: 'Person', panelParagraph: '', panelStyle: 'info'} %}
<div class="row row-border">
    <div class="col-10 card">
        {{ 'Family Children Details'|trans }}
    </div>
    <div class="col-2 card collection-actions">
        {{ 'actions'|trans({}, 'System') }}
    </div>
</div>
{{ collection_widget(form.childMembers) }}
{% include 'Default/panelEnd.html.twig' %}