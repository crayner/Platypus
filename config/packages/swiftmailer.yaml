swiftmailer:
    url: '%env(MAILER_URL)%'
    transport: '%mailer_transport%'
    host: '%mailer_host%'
    username: '%mailer_user%'
    password: '%mailer_password%'
    spool: '%mailer_spool%'
    delivery_addresses: {  }
    disable_delivery: false
